import React from "react";
import PropTypes from "prop-types";

const Feed = () => {
  return (
    <p>FEED * * *</p>
    // display the food coming down
  );
};

Feed.propTypes = {
  foodLocation: PropTypes.number
};

export default Feed;