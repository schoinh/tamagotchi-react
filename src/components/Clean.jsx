import React from "react";
import PropTypes from "prop-types";

const Clean = () => {
  return (
    <p>Clean * * *</p>
    // display the clean coming down
  );
};

Clean.propTypes = {
  foodLocation: PropTypes.number
};

export default Clean;