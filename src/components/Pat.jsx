import React from "react";
import PropTypes from "prop-types";

const Pat = () => {
  return (
    <p>Pat * * *</p>
    // display the pat coming down
  );
};

Pat.propTypes = {
  foodLocation: PropTypes.number
};

export default Pat;